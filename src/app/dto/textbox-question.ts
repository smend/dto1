import { QuestionBase } from './question-Base';
export class TextboxQuestion extends QuestionBase {
  controlType = 'textbox';

  // for visabiltiy, example: show q 1,3,7 if the numer is within 30-60
}
